[project]
name = "what-are-embeddings"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "transformers>=4.48.1",
]

[dependency-groups]
dev = [
    "rich>=13.9.4",
    "ruff>=0.9.3",
]
